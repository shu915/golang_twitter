{
  "name": "golang_twitter",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "sass static/scss/style.scss static/css/style.css --no-source-map",
    "watch:css": "sass --watch static/scss:static/css --no-source-map"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "sass": "^1.89.2"
  }
}
